/* function multiplyTwoNumber(first, second) {
  return first * second;
}

console.log(multiplyTwoNumber(10, 15)); */

// function expression
/* const multiply = function (first, second) {
  return first * second;
};

console.log(multiply(5, 4)); */

// arrow function
const multiply = (first, second) => first * second;
console.log(multiply(4, 50));
